# 本地预览方法
```
yarn global add http-server
hs . -c-1
```

```
echo "# canvas-demo1" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin git@github.com:ioudan/canvas-demo1.git
git push -u origin main
```